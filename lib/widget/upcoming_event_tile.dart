import 'package:flutter/material.dart';

class upcoming_event_tile extends StatelessWidget {
  const upcoming_event_tile({Key? key, this.upcomingEventData})
      : super(key: key);
  final dynamic upcomingEventData;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
